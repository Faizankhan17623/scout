VITE_API_URL=http://localhost:5000/api

# Secret path for the hidden admin dashboard, e.g. /ctrl-<random hex>.
# Pick your own random value and keep it private — do not share or commit
# the real value used in production.
VITE_ADMIN_PATH=
